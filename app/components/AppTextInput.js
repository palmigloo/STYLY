import React from "react";
import { StyleSheet, View } from "react-native";

function AppTextInput({ children, style }) {
  return <View></View>;
}

const styles = StyleSheet.create({});
export default AppTextInput;
